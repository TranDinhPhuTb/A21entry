<div class="menu">
	<div>
		<%= link_to "Home", root_path %> | 
		<%= link_to "Users", custom_users_path %> | 
		<%= link_to "Teams", teams_path %> | 
		<%= link_to "Matchs", matchs_path %> | 
	<% if user_signed_in? %>
	  You are <%= current_user.first_name || "-" %> <%= link_to('Logout', destroy_user_session_path, method: :delete) %>        
	<% end %>
  </div>
</div>
